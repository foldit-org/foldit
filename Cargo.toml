[workspace]
resolver = "2"
members = [
    "crates/foldit-frontend",
    "crates/foldit-render",
    "crates/foldit-conv",
    "crates/foldit-runner",
    "xtask",
]

[package]
name = "foldit-rs"
version = "0.1.0"
edition = "2021"
description = "A reimagined Foldit decoupled into GUI, render engine, and backends for Rosetta and ML"
default-run = "foldit-rs"

# Main application binary
[[bin]]
name = "foldit-rs"
path = "src/main.rs"

# Runner worker binary - source lives in foldit-runner, but defined here so
# `cargo build` from workspace root builds it automatically
[[bin]]
name = "foldit-runner-worker"
path = "crates/foldit-runner/src/bin/worker.rs"

[dependencies]
# Workspace crates
foldit-frontend = { path = "crates/foldit-frontend" }
foldit-render = { path = "crates/foldit-render" }
foldit-conv = { path = "crates/foldit-conv" }
foldit-runner = { path = "crates/foldit-runner", default-features = false, features = ["python"] }

# Required for foldit-ml-worker binary
anyhow = "1.0"

# Async for background ML tasks
tokio = { version = "1", features = ["rt-multi-thread", "sync", "macros", "time"] }

# Core
glam = "0.30"
winit = "0.30"
pollster = "0.4"
bytemuck = { version = "1.21", features = ["derive"] }
wgpu = "28"

# Direct mmCIF parsing for atom names and residue info
pdbtbx = "0.12"

# Utilities
log = "0.4"
env_logger = "0.11"
lazy_static = "1.4"

# HTTP client for downloading from RCSB
reqwest = { version = "0.11", features = ["blocking"] }

# Embedded webview (lightweight replacement for Tauri)
wry = { version = "0.50", features = ["transparent"] }

# Serialization for IPC and FrontendState
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
base64 = "0.22"

[patch."https://github.com/petridecus/foldit-conv"]
foldit-conv = { path = "crates/foldit-conv" }
